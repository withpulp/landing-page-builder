// Define App Constants

if (Meteor.App) {
  throw new Meteor.Error('Meteor.App already defined? see client/lib/constants.js');
}

Meteor.App = {
  NAME: 'Landing Page Builder',
  DESCRIPTION: 'Landing Page Builder app built with Meteor.'
};
